## Реализовать функцию, которая будет сумировать числа и выводить логи
```ts
// Пример
// sum(5)(4)(11)
// 5 9 20
```
>[!faq]- Ответ
> ```ts
> function sum(value: number) {
> 	console.log(value);
> 	return function(newValue: number) {
> 		return sum(value + newValue);
> 	}
> }
> sum(5)(4)(11);
> ```
## Функция принимает два объекта с различными ключами. Необходимо вернуть первый объект с обновленными ключами из второго объекта (Только те, которые совпадают)
```ts
// Функция принимает два объекта с различными ключами
// Необходимо вернуть первый обхект с обновленными ключами из второго объекта
// Только те, которые совпадают
  
// Пример
const obj1 = {
	foo: 'foo',
	bar: 'bar'
}

const obj2 = {
	bar: 'foo',
	some: 'some'
}

const result = {
	foo: 'foo',
	bar: 'foo'
}
```
> [!faq]- Ответ
> 
> ```ts
> const mergeSomeKyesOfObject = (obj1: UnknownObject, obj2: UnknownObject) => {
> 	const copiedObject: UnknownObject = structuredClone(obj1);
> 	for (let key in copiedObject) {
>         if (key in obj2) {
>             copiedObject[key] = obj2[key]
>         }
>     }
>     return copiedObject;
> }
> ```

## Реализовать функцию, которая принимает массив значений и колбэк по результату которого будут группироваться значения
```ts 
// Пример
const arr = [6.1, 6.3, 4.2];
const callbackFunc = Math.floor;

const result = {
    '4': [4.2],
    '6': [6.1, 6.3]
}
```
> [!faq]- Ответ
> ```ts
> const groupBy = (arr: number[], callbackFunc: (value: number) => number) => {
>     const map = new Map();
>     
>     arr.forEach((value: number) => {
>         const callbackResult: number = callbackFunc(value);
>         const isAlreadyExistedinMap = map.has(callbackResult);
>         isAlreadyExistedinMap ? map.get(callbackResult).push(value) : map.set(callbackResult, [value]);
>     });
> 
>     return Object.fromEntries(map);
> }
> 
> console.log(groupBy(arr, callbackFunc));
> ```

## Написать метод delay для функции, который вызовет заданную функцию спустя какое то количество времени с заданными аргументами
```ts
const START = Date.now();

function someFn() {
    console.log('time', Date.now() - START);
    console.log('args', arguments);
}

const f = someFn.delay(1000);
f('arg1', 'arg2', 1, 2);
```
> [!faq]- Ответ
> 
> ```ts
> Function.prototype.delay = function(timeMs) {
>   return (...args) => { // обратить внимание на стрелочную функцию (сохраняем контекст + параметры функции)
>     setTimeout(() => {
>       this.call(this, ...args); // вызов функции через call, чтобы сохранить ее параметры
>     }, timeMs);
>   }
> }
> ```

## Найти сумму всех элементов небинарного дерева. Использовать рекурсивный метод и итеративный метод
```js
const tree = {
  v: 5,
  c: [
    {
      v: 10,
      c: [
        {
          v: 11
        }
      ],
    },
    {
      v: 7,
      c: [
        {
          v: 5,
          c: [
            {
              v: 1
            }
          ]
        }
      ]
    },
    {
      v: 5,
      c: [
        {
          v: 10,
        },
        {
          v: 15
        }
      ]
    }
  ]
}
```
> [!faq]- Ответ
> 
> ```js
> const recursive = (tree) => {
>   if (!tree.hasOwnProperty('c')) { return tree.v; }
>   
>   let sum = tree.v;
>   tree.c.forEach(node => {
>     sum += recursive(node);
>   })
>   
>   return sum;
> }
> 
> const iteration = (tree) => {
>   if (!tree.c.length) { return tree.v || 0; }
>   
>   const stack = [];
>   let sum = tree.v;
>   
>   tree.c?.forEach(node => {
>     stack.push(node);
>   });
>   
>   while (stack.length) {
>     const node = stack.pop();
>     sum += node.v;
>     node['c']?.forEach(n => {
>       stack.push(n);
>     })
>   }
>   
>   return sum;
> }
> ```
## Заменить вхождения строки на новую строку
```js
function replaceSubstring(str, search, replace) {}
```
> [!faq]- Ответ
> 
> ```js
> function replaceSubstring(str, search, replace) {  
>   return str.split(search).join(replace);  
> }
> 
> // or
> 
> function replaceSubstring(str, search, replace) {  
>   let result = '';  
>   
>   for (let i = 0; i < str.length; i++) {  
>     if (str.substring(i, i + search.length) === search) {  
>       result += replace;  
>       i += search.length - 1;  
>     } else {  
>       result += str[i];  
>     }  
>   }  
>   
>   return result;  
> }
> ```

## Проверить строку на полиндром
> [!faq]- Ответ
> ```ts
> function isPalindrome(str) {
>   const cleaned = removeCommasAndSpaces(str).toLowerCase();
>   for (let i = 0, j = cleaned.length - 1; i !== j; i++, j--) {
>     if (cleaned[i] !== cleaned[j]) {
>       return false;
>     }
>   }
>   
>   return true;
> }
> 
> function removeCommasAndSpaces(str) {
>   return str.split(',').join('').split(' ').join('');
> }
> 
> console.log(isPalindrome('A man, a plan, a canal, Panama')) // true
> console.log(isPalindrome('hello world')) // true
> ```

## Полифилл для arr.filter()
> [!faq]- Ответ
> ```js
> Array.prototype.myFilter = function(callbackFn) {
>   const result = [];
>   for (let i = 0; i < this.length; i++) {
>     if (callbackFn(this[i], i, this)) {
>       result.push(this[i]);
>     }
>   }
>   
>   return result;
> }
> ```

## Восстановить маршрут по билетам
```js
console.log(reconstructTrip([
  {from: 'Спб', to: 'Минск'},
  {from: 'Киев', to: 'Новосибриск'},
  {from: 'Череповец', to: 'Москва'},
  {from: 'Минск', to: 'Киев'},
  {from: 'Москва', to: 'Спб'},
]));
```
> [!faq]- Ответ
> ```js
> function reconstructTrip(tickets) {
>   const map = new Map();
>   
>   for (let ticket of tickets) {
>     map.set(ticket.from, ticket.to);
>   }
>   
>   let start = [...map.keys()].find(from => ![...map.values()].includes(from));
>   
>   const routes = [];
>   for (let i = 0; i < tickets.length; i++) {
>     if (!start) { break; }
>     routes.push({from: start, to: map.get(start)});
>     start = map.get(start);
>   }
>   
>   return routes;
> }
> ```

## Написать функцию промисификации
```js
function exampleCallbackFunction(a, b, callback) {
  if (a < 0 || b < 0) {
    callback(new Error('Аргументы должны быть неотрицательными'));
  } else {
    callback(null, a + b);
  }
}

const examplePromiseFunction = promisify(exampleCallbackFunction);

examplePromiseFunction(1, 2)
  .then(result => console.log(result))
  .catch(error => console.error(error));
```
> [!faq]- Ответ
> ```ts
> function promisify(fn) {
>   return function (...args) {
>     console.log(...args);
>     return new Promise((resolve, reject) => {
>       fn(...args, (err, result) => {
>         if (err) { return reject(err); }
>         resolve(result);
>       })
>     })
>   }
> }
> ```
## Объединение интервалов
```js
const input1 = [[1, 3], [2, 6], [8, 10], [15, 18]]; // [[1, 6], [8, 10], [15, 18]]  
const input2 = [[1, 3], [2, 6], [5, 10], [15, 18]]; // [[1, 10], [15, 18]]  
const input3 = [[1, 4], [4, 5]] // [1, 5]  
const input4 = [[1, 15], [4, 10], [20, 25], [27, 28]]; // [[ 1, 15 ], [ 20, 25 ], [ 27, 28 ]]
```
> [!faq]- Ответ
> ![[Pasted image 20260727110041.png]]
> ```js
> function mergeIntervals(intervals) {  
>   const mergedIntervals = [intervals[0]];  
>   
>   for (const interval of intervals) {  
>     const recent = mergedIntervals[mergedIntervals.length - 1];  
>     if (recent[1] >= interval[0]) {  
>       recent[1] = Math.max(interval[1], recent[1]);  
>     } else {  
>       mergedIntervals.push(interval);  
>     }  
>   }  
>   
>   return mergedIntervals;  
> }
> ```

