### Описание
Метод массива `forEach()` позволяет применить [[Callback (функция обратного вызова) (Колбэк-функция)|колбэк-функцию]] ко всем элементам массива. Можно использовать вместо классического цикла `for`. В отличие от него `forEach()` выглядит более читабельным и понятным.
```js
const persons = [  
    { name: 'Egor', age: 24, budget: 40000 },  
    { name: 'Vitaliy', age: 25, budget: 21000 },  
    { name: 'Sasha', age: 34, budget: 18000 },  
    { name: 'Maksim', age: 20, budget: 10000 },  
    { name: 'Andrey', age: 52, budget: 10000 },  
    { name: 'Dima', age: 31, budget: 25000 },  
    { name: 'Artem', age: 18, budget: 17000 },  
]  
  
for (let i = 0; i < persons.length; i++) {  
    console.log(persons[i]);  
}  
  // or
for (const person of persons) {  
    console.log(person);  
}  
  // or
persons.forEach(person => {  
    console.log(person);  
})  
  // or
persons.forEach(person => console.log(person));
```
### Параметры
Для того чтобы использовать `forEach()`, понадобится [[Callback (функция обратного вызова) (Колбэк-функция)|колбэк-функция]], которую необходимо передавать в метод. Функцию можно объявить отдельно:
```js
function sliceFruit(fruit) {  
    console.log('Разрезаю ' + fruit + '!');  
}  
  
const fruits = ['🍎', '🍊', '🍋', '🍓', '🥝'];  
  
fruits.forEach(sliceFruit);
```

Или создать её прямо в месте вызова:
```js
const fruits = ['🍎', '🍊', '🍋', '🍓', '🥝'];  
  
fruits.forEach(fruit => {  
    console.log('Разрезаю ' + fruit + '!');  
});
```

Важно знать, какие параметры принимает [[Callback (функция обратного вызова) (Колбэк-функция)|колбэк-функция]]. Всего их три:
- `item` — элемент массива в текущей итерации;
- `index` — индекс текущего элемента;
- `arr` — сам массив, который мы перебираем.
### Возвращаемое значение
Метод `forEach()` ничего не возвращает.
### Дополнительно
Если изменять в forEach() объекты, то этот объект будет изменяться в исходном массиве. Это связано с [[Хранение переменных по ссылке и по значению|Хранением переменных по ссылке и по значению]]. Изменение примитивов ни к чему не приведет (для этого используется метод [[map()]]). 
```js
сonst persons = [  
    { name: 'Egor', age: 24, budget: 40000 },  
    { name: 'Vitaliy', age: 25, budget: 21000 },  
    { name: 'Sasha', age: 34, budget: 18000 },  
    { name: 'Maksim', age: 20, budget: 10000 },  
    { name: 'Andrey', age: 52, budget: 10000 },  
    { name: 'Dima', age: 31, budget: 25000 },  
    { name: 'Artem', age: 18, budget: 17000 },  
]  
  
persons.forEach(person => {  
    person.name = 'Ivan';  
});  
  
console.log(persons); // у всех будет имя 'Ivan'

const nums = [1, 2, 3, 4, 5, 6];  
  
nums.forEach(num => num += 10);  
  
console.log(nums); // [1, 2, 3, 4, 5, 6]
```
>[!error] return в `forEach`
> return в методе `forEach` не прекращает выполнение цикла и не возвращает результат, а просто пропускает текущую итерацию
> ```js
> const fruits = ['🍎', '🍊', '🍋', '🍓', '🥝'];  
>fruits.forEach((fruit, index, arr) => {  
 >    if (index === 1) { return; }  
>  console.log('Разрезаю ' + fruit + '!');  // 0, 2, 3, 4
>});
> ```

>[!note] Цикл `for..of` лучше чем `forEach`
>Мое личное мнение, что между `forEach` и `for..of` лучше выбрать `for..of` потому что он дает больше возможностей (например `break` и `continue`, в записи не намного длиннее и может все то же, что и `forEach`.

