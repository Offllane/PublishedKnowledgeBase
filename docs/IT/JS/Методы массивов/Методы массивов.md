## [[Array.isArray()]]

## [[forEach()]] - лучше использовать  for..of

## [[map()]] - НЕ изменяет исходный массив

## [[reduce()]] - НЕ изменяет исходный массив

## reduceRight() - НЕ изменяет исходный массив.аш

## [[Array.fill()|fill()]] - изменяет исходный массив

## keys()

## values()

## entries()

## sort() - изменяет исходный массив

## toSorted() - не изменяет исходный массив

## [[Array.filter()]] - не изменяет исходный массив

## at() - получаем значение по индексу at(-1)

## [[Array.find()]] - возвращает первый элемент по предикату

## findIndex() - вовзвращает первый индекс по предикату

## flat() - НЕ изменяет исходный массив

## flatMap() НЕ изменяет исходный массив

## every()

## some()

## includes() - имеет опциональный параметр для отметки откуда начинать

## indexOf()

## lastIndexOf()

## reverse()

## toReversed()

## push() - добавляет элемент(ы) в конец и возвращает новую длину массива

## pop() - удаляет последний элемент из массива и возвращает удаленное значение

## shift() - удаляет первый элемент из массива и возвращает его значение

## unshift() - добавляет элемент(ы) в начало массива и возвращает новую длину массива

## [[Array.slice()]] - НЕ изменяет исходный массив. Копирует часть элементов.

## [[Array.splice()]] - изменяет исходный массив. Возвращает массив удаленных элементов

## [[Array.toSpliced()]] - НЕ изменяет исходный массив. Возвращает новый массив, удаляя элементы

## with() - НЕ изменяет исходный массив. Заменяет элемент в массиве по индексу

## [[concat()]] - НЕ изменяет исходный массив. Объединяет несколько массивов

## join()



